source:      .
destination: ./_site
includes:    ./_includes
url: "https://ai.stanford.edu"   # canonical host for this (Stanford-served) build
baseurl: "/~orrzohar"            # Stanford user-dir path; orrzohar.github.io is now redirect-only

collections:
  projects:
    output: true
